# DB info
database = dict(
        url = 'db.example.com', # enter url of DB 
        passwd = 'password' # enter DB password 
        )

# script will check here to see what url and port to check for internet connection
conn = dict(
        url = 'google.com', # enter url
        port = '80' # enter port
        )

# temp storage file
csv = 'data_file.csv' # data will be stored here until insert to DB is possible
