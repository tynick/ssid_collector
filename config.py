# DB info
database = dict(
        url = 'db.example.com',
        name = 'ssiddb',
        table = 'ssids',
        user = 'username',
        passwd = 'password'
        )

# script will use this to see what url and port to check for internet connection
conn = dict(
        url = 'google.com', # enter url
        port = '80' # enter port
        )

# temp storage file
csv = 'data_file.csv'
